//console.log('In model');

var app = app || {};

app.wikiModel=Backbone.Model.extend({ });

app.wikiSuggestionModel=Backbone.Model.extend({ });

app.wikiPreviousSearchModel=Backbone.Model.extend({ });